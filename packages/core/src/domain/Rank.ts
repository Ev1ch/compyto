/**
 * Process unique rank
 * which allows to identify the {@link core/src.Process | Process}
 * and communicate with it.
 * Master process must have rank = 0.
 */
type Rank = number;

export default Rank;

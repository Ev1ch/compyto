/**
 * The interface represents a Uniform Resource Identifier (URI).
 * It provides a structured way to describe a resource's location.
 */
export default interface URI {
  path: string;
  port: number;
}

/**
 * Types which can be used in transfer
 * methods between different processes.
 */
enum Type {
  BOOLEAN = 'boolean',
  NUMBER = 'number',
  STRING = 'string',
  OBJECT = 'object',
}

export default Type;
